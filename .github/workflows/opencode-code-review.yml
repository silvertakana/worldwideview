name: OpenCode Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
    paths-ignore:
      - '**/*.md'
      - '.gitignore'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  opencode-review:
    if: contains('["OWNER","MEMBER","COLLABORATOR"]', github.event.pull_request.author_association)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Run OpenCode Code Review
        uses: anomalyco/opencode/github@v1.17.13
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          model: deepseek/deepseek-v4-flash
          use_github_token: true
          share: false
          prompt: |
            You are a code reviewer for the WorldWideView repository (${{ github.repository }}).
            The current PR is #${{ github.event.pull_request.number }} and its changes are checked out in the workspace.

            Review the changed files for:
            - Bugs and edge cases
            - Security issues
            - Code quality and maintainability
            - Adherence to project conventions (see AGENTS.md and CLAUDE.md in the repo root)

            Before posting, list existing reviews by github-actions[bot] on this PR (gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/reviews) and DELETE any you find (gh api -X DELETE repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/reviews/<id>) so only this latest review remains.

            Post your review as a PR review comment:
              gh pr review ${{ github.event.pull_request.number }} --repo ${{ github.repository }} --comment --body "<your findings>"
